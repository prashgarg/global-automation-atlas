# Prompt version notes

The paper uses the v6b automation-exposure prompt family. The country-conditioned, income-group, and context-free prompts are supplied here exactly as local protocol files. The public replication package starts from retained labels rather than rerunning the API step.

